create table students(
  student_id INT PRIMARY KEY,
  name VARCHAR(50),
  age INT,
  gender CHAR(5),
  city VARCHAR(30),
  course VARCHAR(50),
  marks INT,
  admission_date DATE,
  status VARCHAR(20)
);